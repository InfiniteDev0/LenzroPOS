NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key

# Where the POS app is deployed — the landing page's "Open the till"
# button. Optional: defaults to the production till
# (https://lenzro-pos-system.vercel.app). Set it to point a deployment at
# a preview build or a custom domain instead.
NEXT_PUBLIC_POS_URL=https://lenzro-pos-system.vercel.app
